# Copy to `.env` and fill in locally. Do not commit `.env`.

# bunq / Open Banking (adjust names to match real SDK or API)
# BUNQ_API_KEY=
# BUNQ_ENV=sandbox

# App
# NODE_ENV=development
# APP_BASE_URL=http://localhost:3000

# Database (if applicable)
# DATABASE_URL=
