# data_ingestion examples

**Input contract:** `CollectedBundle`  
**Output contract:** `EvidenceBundle`

| File | Role |
|------|------|
| `artifact_bundle.json` | Raw collection artifact (same shape as collection examples). |
| `expected_evidence.json` | What the **stub** ingester must produce today; swap body when LLM is wired, keep schema. |
