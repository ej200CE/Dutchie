# evidence_aggregation examples

**Input:** `EvidenceBundle`  
**Output:** `GraphBlueprint` (`operations`: `add_node` / `add_edge`)

| File | Role |
|------|------|
| `artifact_evidence.json` | Output of ingestion for the weekend scenario. |
| `expected_blueprint.json` | Node/edge ops the **stub** aggregator emits. |
