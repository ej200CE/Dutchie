# llm examples

| File | Role |
|------|------|
| `sample_messages.json` | Example chat payload for the dev UI / tests. |
| `expected_stub_response.json` | What **`StubLLMClient`** returns for that payload (echo prefix). |
