# data_collection examples

**Contract:** `CollectedBundle` — see `billion_hackathon.contracts.collected`.

| File | Meaning |
|------|---------|
| `artifact_bundle.json` | Example payload after a user added one note (no binary upload in JSON). |

There is no separate `expected` file here: collection is a **sink**; the next module defines expected evidence for the same story.
