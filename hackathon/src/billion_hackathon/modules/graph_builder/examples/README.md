# graph_builder examples

**Input:** `GraphBlueprint`  
**Output:** graph snapshot (`nodes`, `edges`) + inconsistency list (often empty).

| File | Role |
|------|------|
| `artifact_blueprint.json` | Same as aggregation output for the weekend story. |
| `expected_graph.json` | Snapshot after applying ops (sorted node order). |
