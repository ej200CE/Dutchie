# computation examples

**Input:** graph snapshot (`nodes`, `edges` — `schema_version` optional)  
**Output:** `compute` result (`success`, `per_person`, `suggested_transfers`, …)

| File | Role |
|------|------|
| `artifact_graph.json` | Graph for the weekend / groceries scenario. |
| `expected_compute.json` | Golden output from `compute()`. |
